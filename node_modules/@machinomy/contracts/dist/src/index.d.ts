import Unidirectional from '../build/wrappers/Unidirectional';
export { Unidirectional };
export declare function randomId(digits?: number): number;
export declare function channelId(sender: string, receiver: string): string;
