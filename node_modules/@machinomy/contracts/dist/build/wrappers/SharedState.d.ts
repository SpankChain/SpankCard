import * as BigNumber from 'bignumber.js';
import * as Web3 from 'web3';
import * as truffle from 'truffle-contract';
export declare namespace SharedState {
    const ARTIFACT: any;
    interface Contract {
        address: string;
        constructor: {
            web3: Web3;
        };
        merkleRoot: {
            (): Promise<string>;
            call(): Promise<string>;
        };
        owner: {
            (): Promise<string>;
            call(): Promise<string>;
        };
        nonce: {
            (): Promise<BigNumber.BigNumber>;
            call(): Promise<BigNumber.BigNumber>;
        };
        isContained: {
            (_merkleRoot: string): Promise<boolean>;
            call(_merkleRoot: string): Promise<boolean>;
        };
        update: {
            (_nonce: number | BigNumber.BigNumber, _merkleRoot: string, options?: Web3.CallData): Promise<truffle.TransactionResult>;
            call(_nonce: number | BigNumber.BigNumber, _merkleRoot: string, options?: Web3.CallData): Promise<void>;
            estimateGas(_nonce: number | BigNumber.BigNumber, _merkleRoot: string): Promise<number>;
            request(_nonce: number | BigNumber.BigNumber, _merkleRoot: string): {
                method: string;
                params: [{
                    to: string;
                    data: string;
                }];
            };
        };
        send: (value: BigNumber.BigNumber | number) => Promise<truffle.TransactionResult>;
        sendTransaction: (opts: Web3.CallData) => Promise<truffle.TransactionResult>;
    }
    function contract(provider?: Web3.Provider, defaults?: Web3.CallData): truffle.TruffleContract<Contract>;
}
export default SharedState;
