"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const truffle = require("truffle-contract");
var PublicRegistry;
(function (PublicRegistry) {
    PublicRegistry.ARTIFACT = require('../contracts/PublicRegistry.json');
    function isDidDeployEvent(something) {
        return something.event === 'DidDeploy';
    }
    PublicRegistry.isDidDeployEvent = isDidDeployEvent;
    function contract(provider, defaults) {
        let instance = truffle(PublicRegistry.ARTIFACT);
        if (provider) {
            instance.setProvider(provider);
        }
        if (defaults) {
            instance.defaults(defaults);
        }
        return instance;
    }
    PublicRegistry.contract = contract;
})(PublicRegistry = exports.PublicRegistry || (exports.PublicRegistry = {}));
exports.default = PublicRegistry;
//# sourceMappingURL=PublicRegistry.js.map